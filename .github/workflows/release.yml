name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
          - platform: windows-latest
            args: ''
          - platform: ubuntu-22.04
            args: '--target x86_64-unknown-linux-gnu'

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            libgit2-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4.4.0
        with:
          node-version: 24

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin' || '' }}

      - name: Install frontend dependencies
        run: npm install

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'CommitCat ${{ github.ref_name }}'
          releaseBody: |
            ## CommitCat ${{ github.ref_name }}

            ### Download

            | 플랫폼 | 파일 | 설명 |
            |--------|------|------|
            | 🍎 macOS (Apple Silicon) | `.dmg` | Mac 설치 파일 (M1/M2/M3/M4) |
            | 🪟 Windows | `.exe` (setup) | Windows 설치 파일 (권장) |
            | 🪟 Windows | `.msi` | Windows MSI 설치 파일 |
            | 🐧 Linux | `.AppImage` | Linux 실행 파일 (대부분의 배포판) |
            | 🐧 Linux | `.deb` | Debian/Ubuntu 패키지 |

            > ⚠️ 현재 Intel Mac은 지원하지 않습니다.

            > **🍎 macOS 사용자 필독:** 앱이 아직 코드사인되지 않아, 처음 실행 시 **"손상되었기 때문에 열 수 없습니다"** 경고가 나올 수 있습니다.
            > 터미널에서 아래 명령어를 실행하면 해결됩니다:
            > ```
            > xattr -cr /Applications/Commit\ Cat.app
            > ```

            ### What's new
            See [CHANGELOG](https://github.com/eunseo9311/commit-cat/commits/main) for details.
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
